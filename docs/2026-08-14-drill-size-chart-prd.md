# PRD — Fastener Drill Reference Table

| 항목 | 내용 |
|------|------|
| 문서명 | 성능기골 스티커 보드 — Fastener Drill Reference Table |
| 버전 | **v1.4 (최종 승인본)** |
| 작성일 | 2026-08-14 |
| 대상 파일 | `info.html` (전면 교체), `assets/photos/` (신규), `index.html` · `style.css` · `app.js` (헤더 개편) |
| 인프라 | GitHub Pages 정적 호스팅, 서버 없음, DB 사용 안 함 |
| 상태 | **1단계 구현 완료** · 2단계(사진 콘텐츠)는 파일 수급 대기 |

---

## 1. 배경

`info.html` 은 Huck Gun · Fastener 관계표를 담고 있었으나, 현장에서 실제로 자주 찾는 것은 **드릴 사이즈**다. 판금 작업 중 "이 리벳엔 몇 번 드릴?", "1/8 인치가 몇 mm?" 를 확인하려면 매번 인쇄물이나 검색에 의존해야 했다.

이를 **번호 드릴 + 분수 드릴을 지름 순으로 통합한 표** 하나로 대체하고, 현장 메모·클리코 색·참고 사진을 항목별로 붙일 수 있게 한다.

## 2. 승인된 결정 사항

| # | 항목 | **결정** |
|---|------|----------|
| 1 | Letter drill (A~Z) | **넣지 않는다** |
| 2 | 추가 열의 성격 | **자유 메모** (형식 제약 없음) |
| 3 | 클리코 색 | 3/32=은색, 1/8=구리색, 5/32=검정, 3/16=금색 **4종만.** 1/4″ 는 **제외** |
| 4 | 팝업 미디어 | **GIF/영상 → 정지 사진으로 변경.** 구조만 먼저 만들고 파일은 나중에 |
| 5 | 테마 | **다크 + 앰버** (다른 페이지와 통일) |
| 6 | 분수 범위 | **1/64″ ~ 1/4″**, 1/64 간격 → **16개** |
| 7 | 기존 Huck 관계표 | **전부 삭제** |
| 8 | 번호 드릴 범위 *(v1.2)* | **#41 이상 제외 → #1 ~ #40.** #40 은 AD3 3/32″ 리벳용이라 되살림 |
| 9 | 차트 모드 레이아웃 *(v1.1)* | 병렬 배치 폐기. **세로 한 줄 목록**으로 스크롤하며 전부 확인. 열은 **표기 · 인치 · 분수 · 메모** 4개 |
| 10 | 페이지 이름 *(v1.3)* | **Fastener Drill Reference Table** |
| 11 | 보드 헤더 *(v1.3)* | **`🟨 스티커 추가` 버튼만 남기고 나머지 전부 `⋯` 더보기로 이동** |
| 12 | 뷰 모드 *(v1.4)* | **차트 모드 폐기. 표 하나만 둔다** |
| 13 | 이름 표기 *(v1.4)* | 안팎을 **`Fastener Drill Reference Table` 로 통일.** 폭이 모자랄 때만 **`FDRT`** 로 줄인다 |

> **4번 변경의 효과:** GIF 대비 사진은 용량이 1/20~1/50 수준(300KB 이하)이라 초안에서 우려했던 저장소 용량·대역폭 문제가 **완전히 해소**됐다. 형식 변환 고민(MP4 권장 등)도 불필요해졌다.
>
> **8번 경위:** 처음엔 "40번 이상"을 문자 그대로 읽어 `MAX_NO = 39` 로 #40 까지 잘라냈으나, #40 은 AD3 3/32″ 리벳용이라 **되살려 `MAX_NO = 40` 으로 확정**했다. 범위 조정은 이 상수 한 줄이면 되고, 데이터 배열에는 #80 까지 그대로 남아 있다.
>
> **9번 배경:** v1.0 의 N단 병렬 배치는 밀도가 높은 대신 `No. | Dia.` 2열밖에 담지 못했다. 메모를 표에서 바로 읽으려면 열이 필요하므로, **"스크롤해도 되니 밑으로 내리면서 다 본다"** 는 요구에 맞춰 단일 목록으로 전환했다.

## 3. 목표 / 비목표

### 목표
1. No.1~40 + 분수 1/64~1/4″ 를 **하나의 지름 순 표**로 통합한다.
2. 인치 소수 ↔ mm ↔ 근접 분수를 **동시에** 보여 환산 단계를 없앤다.
3. 모바일에서 **위에서 아래로 스크롤하며 전체를 훑을 수** 있게 한다.
4. 항목별 **자유 메모 · 클리코 색 · 참고 사진**을 붙일 수 있는 구조를 만든다.
5. 사진 파일이 **하나도 없어도 표가 완전히 동작**해야 한다. (단계적 채우기)

### 비목표
- Letter drill, 탭 드릴, 나사 하부 구멍 표 (v2 이관)
- RPM·이송 계산
- Firebase 연동 / 사용자별 저장 — 읽기 전용 참조표
- 로그인 요구 — `divider.html` 과 동일하게 불필요

## 4. 핵심 설계 — 표 하나, 폭에 따라 열이 접힌다

v1.0~v1.3 은 **차트 모드(세로 목록 4열)** 와 **상세 모드(8열)** 를 토글로 오갔다. 두 모드가 사실상 같은 데이터를 보여주는 데다, 폰에서는 늘 차트 모드만 쓰게 되어 **차트 모드를 없애고 표 하나로 합쳤다.**

대신 **화면 폭에 따라 열이 접힌다.** 접힌 열의 값은 행을 탭했을 때 뜨는 하단 시트에서 모두 볼 수 있다.

| 화면 폭 | 보이는 열 |
|---------|-----------|
| **≤767px (폰)** | `표기` · `인치(또는 mm)` · `근접 분수` · `메모` — **4열** |
| **≥768px** | `구분` · `표기` · `inch` · `mm` · `근접 분수` · `메모` · `클리코` · `사진` — **8열** |

```
폰 (375px)                         PC (1280px)
표기   INCH   근접분수  메모        구분 표기  inch  mm   근접분수       메모           클리코 사진
#21   .1590  5/32″   AD5 5/32″ 🖼   No. #21  .1590 4.04 5/32″(+.0028) AD5 5/32″ 리벳 ●검정  🖼
5/32″ .1563    —       —           Frac 5/32″ .1563 3.97 —            —              —     —
#30   .1285  1/8″    AD4 1/8″ 🖼    No. #30  .1285 3.26 1/8″(+.0035)  AD4 1/8″ 리벳  ●구리  🖼
```

- 번호와 분수가 **지름 순으로 섞여** 표시되므로 "이 지름 근처에 뭐가 있나"를 바로 본다.
- 폰에서는 근접 분수의 **오차 `(+.0035)` 가 접히고** 분수만 남는다. 오차는 시트에서 본다.
- 폰에서는 사진 열이 접히는 대신 **메모 뒤에 `🖼`** 가 붙는다.
- **아무 행이나 탭하면 하단 시트** — mm · 근접 분수 오차 · 클리코 · 사진까지 전부.
- 위에서 아래로 스크롤하면 56행이 전부 지나간다 (폰 약 3.2 화면).

### 4-1. 표 헤더 고정 — v1.3 버그 수정

v1.3 까지 폰에서 **헤더 행이 표 안쪽으로 밀려 내려가 행 위에 겹쳐 보이는 버그**가 있었다.

- **원인**: 표를 감싼 `.table-wrap` 에 `overflow-x:auto` 가 걸려 있었다. `overflow` 가 붙으면 그 요소가 **스크롤 컨테이너**가 되고, 안쪽 `position:sticky` 는 뷰포트가 아니라 **그 컨테이너 기준**으로 붙는다. 헤더에 준 `top:188px` 이 컨테이너 안쪽 188px 지점으로 해석돼 헤더가 표 중간에 떠 있었다.
- **해결**: `overflow` 를 아예 걷어냈다. 대신 좁은 화면에서 **열을 접어 표가 화면 폭에 들어가게** 만들어 가로 스크롤 자체를 없앴다.
- 결과: 280~1280px 전 구간에서 헤더가 컨트롤 바 바로 아래에 정확히 붙는다(오차 0px). 가로 스크롤도 없다.

## 5. 데이터 사양

### 5.1 행 — 총 56개

| 분류 | 개수 | 범위 | 비고 |
|------|:----:|------|------|
| Number drill | **40** | #1 (.2280) ~ **#40 (.0980)** | 제공 데이터. **ANSI 표준값과 전량 대조 완료.** #41~#80 은 `MAX_NO` 로 제외 |
| Fraction drill | **16** | 1/64″ (.0156) ~ 1/4″ (.2500) | 코드에서 `n/64` 로 생성 → 오타 불가 |

> 데이터 배열에는 **#80 까지 그대로 보존**돼 있고 `MAX_NO = 40` 으로 잘라 쓴다. 숫자 하나만 바꾸면 범위가 넓어진다.

### 5.2 열 — 8개

| # | 열 | 출처 | ≥768px | ≤767px (폰) |
|---|-----|------|:------:|:-----------:|
| 1 | 구분 (No./Frac) | 자동 | ✅ | 접힘 → 시트 |
| 2 | **표기** (`#30`, `1/8″`) | 자동 | ✅ | **✅** |
| 3 | **인치** (토글 시 mm) | 데이터 | ✅ | **✅** |
| 4 | mm | 자동 (×25.4) | ✅ | 접힘 → 시트 |
| 5 | **근접 분수** | 자동 | ✅ (오차 포함) | **✅** (오차만 접힘) |
| 6 | **자유 메모** | 입력 | ✅ | **✅** |
| 7 | 클리코 색 | 입력 | ✅ | 접힘 → 시트 |
| 8 | 사진 | 파일 | 🖼 버튼 | 메모 뒤 `🖼` → 시트 |

> 인치 열은 `inch|mm` 토글로 전환된다(헤더도 `inch` ↔ `mm` 으로 바뀜).

### 5.3 데이터 구조

`info.html` 안 인라인 JS. (외부 JSON `fetch` 는 `file://` 로 열 때 CORS 로 막히므로 인라인이 안전)

```js
const EXTRA = {
  "#40": { memo:"AD3 3/32″ 리벳", cleco:"silver", photo:"drill-40.jpg" },
  "#30": { memo:"AD4 1/8″ 리벳",  cleco:"copper", photo:"drill-30.jpg" },
  "#21": { memo:"AD5 5/32″ 리벳", cleco:"black",  photo:"drill-21.jpg" },
  "#11": { memo:"AD6 3/16″ 리벳", cleco:"brass",  photo:"drill-11.jpg" }
};
```

- 키는 표에 나오는 **표기와 동일**해야 한다. 분수는 `″` (U+2033) 사용.
- 필드는 전부 선택. 없는 항목은 표에 자동으로 표시되지 않는다.
- 초기 4개는 리벳 규격이 확정된 항목만 넣었다. **불확실한 값은 채우지 않았다** — 잘못된 드릴을 잡으면 소재를 버리기 때문.

## 6. 사진 팝업 사양

### 6.1 동작
1. 표의 `🖼` 버튼(PC) 또는 하단 시트의 `사진 보기` 를 누른다
2. 중앙 모달 + 배경 흐림, 제목(표기 · 메모) / 사진 / 설명(선택) 구성
3. 닫기: `✕` · 배경 탭 · `ESC`
4. 사진은 **모달을 여는 순간 처음 로드** → 초기 페이지 로딩과 무관
5. **파일이 없으면** 깨진 이미지 대신 "사진 준비 중입니다 + 넣어야 할 경로" 안내 카드

### 6.2 파일 규칙 (`assets/photos/`)

| 항목 | 권장 | 최대 |
|------|------|------|
| 형식 | `.jpg` (사진) / `.png` (도해) | `.webp` |
| 가로 해상도 | 1000~1400px | 1920px |
| 파일당 | **300KB 이하** | 1MB |
| 전체 합계 | 10MB 이하 | 30MB |

> 이 저장소는 이미 62MB 다. 폰 원본(4~8MB)을 그대로 올리면 열람이 느려지므로 리사이즈·압축 후 올린다. 자세한 절차는 `assets/photos/README.md`.

### 6.3 사진 추가 절차 (2단계)
1. 파일을 `assets/photos/` 에 넣는다
2. `info.html` 의 `EXTRA` 에 `photo:"파일명.jpg"` 한 줄을 적는다

## 7. 검색 · 필터 · 정렬

| 기능 | 동작 |
|------|------|
| 검색 | `30` · `.128` · `1/8` · `3.26` · `리벳` · `구리색` 전부 매칭 |
| **지름 역검색** | 숫자 입력 시 **최근접 드릴 3개**를 오차와 함께 상단에 표시. 누르면 상세로 이동 |
| 단위 해석 | 입력값 **1.0 이하 = inch, 초과 = mm** |
| 필터 칩 | 전체 / No. / 분수 / 메모 있음 / 사진 있음 |
| 정렬 | 지름 큰 순(기본) · 작은 순 · 번호 순 |
| 단위 토글 | inch ↔ mm (인치 열과 헤더에 즉시 반영) |

**검증 예시**
- `3.2` 입력 → `1/8″(−.0010)` · `#30(+.0025)` · `#31(−.0060)`
- `1/8` 입력 → `1/8″ 일치` · `#30(+.0035)` · `#31(−.0050)`

## 8. 표기 규칙

| 항목 | 형식 | 예 |
|------|------|-----|
| 인치 | 소수 4자리, 앞 0 생략 | `.1285` |
| mm | 소수 2자리 | `3.26` |
| 근접 분수 | 1/64 스냅 + 기약분수 + 부호 오차 | `1/8″ (+.0035)` |
| 오차 부호 | 드릴이 분수보다 크면 `+` | `+`/`−` |

- 분수 행의 근접 분수는 자기 자신이므로 `—`.
- 숫자는 전부 고정폭(`IBM Plex Mono`)으로 자릿수를 맞춘다.

## 9. 화면 · 반응형

```
┌ 상태바   ● Fastener Drill Reference Table            08:23:09
├ 툴바     [← 보드로]  Fastener Drill Reference Table   [56 / 56 개]
├ 컨트롤   [🔍 검색__________________] [inch|mm] [정렬 ▾]           ← sticky
│          (전체) (No.) (분수) (메모 있음) (사진 있음)
├ 최근접   최근접 드릴 — 입력 3.20mm …                    (검색 시에만)
├ 표       한 개 (폰 4열 / PC 8열, §4)                            ← 헤더 sticky
└ 범례     🖼 표시 의미 · 클리코 색 4종 · 단위 해석 규칙
      ┌ 하단 시트 (행 탭) ┐   ┌ 사진 팝업 ┐
```

- **body 가로 스크롤 없음** — 375px 검증 완료
- 상태바·컨트롤 바 sticky. 표 헤더는 그 아래 높이에 맞춰 자동 정렬(`--sticky-top` 을 실제 높이에서 계산)
- 하단 시트: 폰은 바텀시트, ≥700px 는 중앙 모달
- 탭 영역 최소 34px

### 9.1 상태바 이름 표기 — v1.4

바깥(메뉴·탭 제목)과 안(상태바)의 이름이 달라 헷갈린다는 지적에 따라 **`Fastener Drill Reference Table` 로 통일**했다.

- 상태바 이름은 **런타임에 실제 폭을 재서** 다 들어가면 전체 이름을, 모자라면 **`FDRT`** 로 바꾼다. 하드코딩한 분기점이 아니라 측정값이라 기기·폰트가 달라도 알아서 맞는다.
- 웹폰트(IBM Plex Mono)는 늦게 도착하면 글자 폭이 달라지므로 `document.fonts.ready` 후 한 번 더 잰다.
- 실측: **360px 이상에서 전체 이름**, 320px 이하에서 `FDRT`. 일반적인 폰(375~430px)은 전체 이름이 나온다.

### 9.2 보드 헤더 (`index.html`) — v1.3

버튼이 5개까지 늘어나 폰에서 줄바꿈되던 헤더를 **2개로 줄였다.**

```
이전:  [🟨 스티커 ▾] [📦 자재소모] [🔄 사역표: 홍길동 ▾] [⋯]
이후:  [🟨 스티커 추가] [⋯]
```

`⋯` 더보기 안의 순서 — 이번 순번(정보) · 전체 사역표 보기 · 자재소모 · **Fastener Drill Reference Table** · FT-200_Guide · 균등분할 계산기 · 전투기 런 게임 · (관리자: 회원 관리 · 로그 조회) · ─── · 전체삭제 · 로그아웃

- 가장 자주 쓰는 **스티커 추가만 한 번에 닿게** 두고, 나머지는 전부 한 곳으로 모았다.
- 파괴적 항목(전체삭제·로그아웃) 앞에 구분선(`.drop-item.sep`)을 둬 오조작을 줄인다.
- 드롭다운 폭은 `min-width 215px / max-width min(300px, 100vw − 24px)` 로, 긴 영문 이름이 들어가도 화면 밖으로 나가지 않는다. (375px 기준 폭 238px, 화면 안)
- 제거된 `stickerMenuBtn` · `dutyMenuBtn` · `dutyCurrentLabel` 관련 코드는 `app.js` 에서 함께 정리했다. 이번 순번 표시는 더보기 상단 `drop-info` 로 유지된다.

## 10. 기술 스택

- **단일 HTML 파일** (`info.html`) + 인라인 `<style>`/`<script>` — 기존 페이지와 동일
- 외부 라이브러리 **없음** (표·모달·검색 전부 직접 구현)
- 테마 토큰은 `remove_list.html` 과 동일 (`--bg #16181c`, `--amber #ffb020`, IBM Plex)
- Firebase·`database.rules.json` 변경 **없음**
- 로그인 불필요

## 11. 구현 범위

**1단계 — 표 골격 ✅ 완료**
- [x] `NUMBER_DRILLS` #1~#40 + 분수 16개 자동 생성 (총 56행)
- [x] 단일 표 (폰 4열 / PC 8열 자동 접기 · sticky 헤더)
- [x] 차트/상세 토글 제거 *(v1.4)*
- [x] 표 헤더가 밀려 내려가던 버그 수정 *(v1.4, §4-1)*
- [x] 상태바 이름 자동 축약 `FDRT` *(v1.4, §9.1)*
- [x] 모드·단위·정렬·필터 토글 + `localStorage` 복원
- [x] mm / 근접 분수(1/64 스냅 + 오차) 자동 계산
- [x] 검색 · 필터 칩 · 정렬 · **지름 역검색(최근접 3개)**
- [x] 하단 시트 (셀 탭 → 전체 정보)
- [x] 다크 + 앰버 테마 적용
- [x] 기존 Huck 관계표 전량 삭제
- [x] `index.html` 메뉴 문구 `📊 관계표 보기` → `📊 Fastener Drill Reference Table`
- [x] 보드 헤더 개편 — `🟨 스티커 추가` 만 남기고 `📦 자재소모` · `🔄 사역표` · `🟨 스티커` 드롭다운을 `⋯` 하나로 통합 (§9.2)

**2단계 — 사진 구조 ✅ 완료 (콘텐츠만 대기)**
- [x] 모달 컴포넌트 (배경 탭 · ESC · 스크롤 잠금 · 위치 복귀)
- [x] lazy 로드 + 로드 실패 시 "준비 중" 안내 카드
- [x] `assets/photos/` 폴더 + 추가 방법 README
- [x] `EXTRA` 에 4개 항목(#40·#30·#21·#11) 자리 확보
- [ ] **실제 사진 파일 배치** ← 사용자 작업

**3단계 — 콘텐츠 확장 (수시)**
- [ ] 자유 메모 추가 입력
- [ ] 사진 추가 항목 선정

## 12. 인수 조건 — 검증 결과 (v1.4 재검증)

| # | 조건 | 기대 | 결과 |
|---|------|------|:----:|
| 1 | 375px 열람 | body 가로 스크롤 없음, 표도 넘치지 않음 | ✅ |
| 2 | 폰 보이는 열 | `표기 / inch / 근접 분수 / 메모` 4열 | ✅ |
| 3 | PC(≥768px) 보이는 열 | 8열 전부 | ✅ |
| 4 | **헤더 고정** | 스크롤 중 헤더가 컨트롤 바 바로 아래 (375·768·1280px 모두 오차 0px) | ✅ |
| 5 | 차트/상세 토글 | 존재하지 않음 | ✅ |
| 6 | 총 행 수 | `56 / 56 개` | ✅ |
| 7 | 폰 `#30` 행 | `#30 · .1285 · 1/8″ · AD4 1/8″ 리벳 🖼` | ✅ |
| 8 | PC `#30` 행 | `No. · #30 · .1285 · 3.26 · 1/8″ (+.0035) · AD4 1/8″ 리벳 · 구리색 · 🖼` | ✅ |
| 9 | 상태바 이름 (≥360px) | `Fastener Drill Reference Table` 전체 표기 | ✅ |
| 10 | 상태바 이름 (≤320px) | `FDRT` 로 자동 축약 | ✅ |
| 11 | 행 탭 | 하단 시트에 mm · 오차 · 클리코 · 사진 버튼 | ✅ |
| 12 | 검색 `3.2` | 최근접 `1/8″ / #30 / #31` 오차 병기 | ✅ |
| 13 | 필터 `사진 있음` | `4 / 56 개` | ✅ |
| 14 | mm 토글 | 헤더 `inch → mm`, `#30` 이 `3.26` | ✅ |
| 15 | 사진 파일 없음 | "사진 준비 중" + 경로 안내 | ✅ |
| 16 | JS 오류 | 없음 (폰트·미배치 사진 요청 제외) | ✅ |

## 13. 확장 아이디어 (v2)

- Letter drill (A~Z) 추가
- 탭 드릴 / 나사 하부 구멍 표
- 자주 쓰는 사이즈 즐겨찾기 상단 고정
- 클리코·리벳 재고를 `remove_list.html` 자재 데이터와 연동
- 드릴 지름을 `divider.html` 균등분할 계산기로 넘기기
- PWA 오프라인 캐시 (사진 포함)

## 14. 운영 메모

- **메모·클리코·사진 추가**는 `info.html` 의 `EXTRA` 한 곳만 고치면 된다. 표 구조나 렌더 코드는 건드릴 필요 없다. 메모는 폰·PC 양쪽 표에 **바로 보인다.**
- **번호 범위 변경**은 `const MAX_NO = 40;` 한 줄. 올리면 작은 드릴이 되살아나고, 내리면 더 잘린다. 데이터 배열은 #80 까지 그대로 있다.
- 클리코 색은 현재 **4종(은색·구리색·검정·금색)** 만 정의돼 있다. 1/4″ 는 확인되지 않아 의도적으로 비워 뒀다 — 확정되면 `CLECO` 객체에 한 줄 추가하면 된다.
- 표의 지름 데이터(`NUMBER_DRILLS`)는 표준 규격이므로 수정할 일이 없다.
